import {RingLoader} from "react-spinners"

const Spinner = () => {
  return (
    <div className="loadingSpinnerContainer">
      <RingLoader className="loadingSpinner" />
    </div>
  )
}

export default Spinner